<?php
/**
 * Серверная конфигурация PHP API (runtime на сервере).
 * НЕ путать с .env (Vite, build-time, фронтенд).
 *
 * Этот файл используется PHP-скриптами: send-telegram.php, admin-auth.php, admin-data.php.
 * На сервере: скопировать как .env.php и заполнить реальными значениями.
 * Файл .env.php НЕ должен попадать в Git.
 */
define('TELEGRAM_BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE');
define('TELEGRAM_CHAT_ID', 'YOUR_CHAT_ID_HERE');

/**
 * Хеш пароля для админ-панели (bcrypt).
 * Сгенерировать: php -r "echo password_hash('ваш_пароль', PASSWORD_BCRYPT);"
 */
define('ADMIN_PASSWORD_HASH', '$2y$10$REPLACE_WITH_REAL_BCRYPT_HASH');
